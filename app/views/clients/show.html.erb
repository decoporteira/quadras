<p style="color: green"><%= notice %></p>

<%= render @client %>

<div>
  <%= link_to t('links.edit'), @client %> |
  <%= link_to t('links.back'), clients_path %> 

  <%= button_to "Apagar cliente", @client, method: :delete, class:"btn-primary" %>
</div>