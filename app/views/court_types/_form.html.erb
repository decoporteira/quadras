<%= form_with(model: court_type) do |form| %>
  <% if court_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(court_type.errors.count, "error") %> prohibited this court_type from being saved:</h2>

      <ul>
        <% court_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :day_price, style: "display: block" %>
    <%= form.text_field :day_price %>
  </div>

  <div>
    <%= form.label :night_price, style: "display: block" %>
    <%= form.text_field :night_price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
